type Query {
    filterByDates(queryParams: [QueryParams]!): [DataMetrics]
    totalClicks(queryParams: [QueryParams]!): Float
    campaignMetrics: [DataMetrics]
    filterByDateSource(queryParams: [QueryParams]!): [DataMetrics]
}

type DataMetrics {
    dataSource: String!
    campaign: String!
    date: String
    clicks: Int
    impressions: Int
    CTR: Float
}

input QueryParams  {
    name: String
    dateFrom: String
    dateTo: String
}